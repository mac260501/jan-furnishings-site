# jan-furnishings

## Development

1. Install Node.js 18+.
2. Install dependencies:
   `npm install`
3. Start the dev server:
   `npm run dev`

## Production Build

1. Build:
   `npm run build`
2. Preview built output:
   `npm run preview`
